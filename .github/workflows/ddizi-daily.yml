name: Generate YabanciDizi M3U Playlist

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  generate-playlist:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install requests
        pip install cloudscraper # YENİ: Cloudscraper kütüphanesini yüklüyoruz

    - name: Run M3U Generator
      run: python yabancidizi_generator.py

    - name: Commit and Push to repository
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"
        
        git add yabancidizi_full.m3u
        
        if git diff --staged --quiet; then
          echo "Değişiklik bulunamadı. Commit atılmayacak."
        else
          git commit -m "Auto-Update: YabanciDizi M3U Playlist [$(date)]"
          git push
        fi